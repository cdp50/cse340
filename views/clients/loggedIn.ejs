<div class="background">

    <% if (title) { %>
        <h1><%= title %></h1>
        <% } else { 
              res.redirect('/') 
        } %>

        <% if (message) { %>
        <p class="loginValid"><%= message %></p>
        <% } %>

        <% if (errors) { %>
            <ul class="notice">
           <% errors.array().forEach(error => { %>
             <li><%= error.msg %></li>
          <%  }) %>
           </ul>1
          <% } %>

    <div class="login-box">


        <% if (name == "Basic") { %>
            <h1>Welcome <%= name %></h1>
        <% } else if(name == "Happy" || name == "Manager") { %>
            <h2>Welcome <%= name %></h2>
            <h3>Inventory Management</h3>
            <a href="/inv/"><p>access</p></a>
        <% } %>
            <p>You're logged in.</p>
    </div>
</div>